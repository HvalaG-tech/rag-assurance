# Versions figées le 6 septembre 2026 (python 3.11+).
streamlit==1.61.1
langchain==1.3.15
langchain-core==1.5.6
langchain-text-splitters==1.1.2
langchain-anthropic==1.5.6
langchain-chroma==1.1.0
chromadb==1.5.9
# Embeddings ONNX (sans PyTorch) : suffisant pour la démo en ligne.
fastembed==0.8.0
# Extraction PDF ; cryptography est requis par pypdf pour les CG chiffrées en AES.
pypdf==6.16.1
cryptography==50.0.1
# Recherche lexicale BM25, fusionnée avec le vectoriel.
rank-bm25==0.2.2
python-dotenv==1.2.3
pytest==9.1.1
ruff==0.16.6
# Reconstitution du corpus (scripts/fetch_corpus.py).
requests>=2.32
